<UserControl x:Class="Lime.Client.TestConsole.Views.SessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vw="clr-namespace:Lime.Client.TestConsole.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignData /DesignData/SessionDesignData.xaml}"   
             d:DesignHeight="700" 
             d:DesignWidth="500">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="150" />
        </Grid.RowDefinitions>


        <Grid Grid.Row="0" Margin="0,0,0,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="73*" />
                <ColumnDefinition Width="20*"/>
            </Grid.ColumnDefinitions>

            <Label Content="Host" />
            <TextBox Grid.Column="1" Text="{Binding HostUri}" Grid.ColumnSpan="2" />

            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Grid.ColumnSpan="2">
                <Button Content="_Open" Margin="0,5,5,5" Padding="15,0" Command="{Binding OpenTransportCommand}" />
                <Button Content="_Close" Margin="5" Padding="15,0" Command="{Binding CloseTransportCommand}" />
            </StackPanel>
        </Grid>

        
        <ScrollViewer Grid.Row="1" Margin="0,5">
            <ListBox ItemsSource="{Binding Envelopes}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <vw:EnvelopeView />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="0,5">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBox FontFamily="Courier New" Text="{Binding InputJson}" />
            <Button Grid.Row="1" Content="_Send" />
        </Grid>

    </Grid>
</UserControl>
